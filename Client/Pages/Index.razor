@page "/"
@page "/{categoryUrl}"
@using BlazorCommerceWebApp.Client.Services.ProductService
@inject IProductService ProductService

<PageTitle>Revolution Market</PageTitle>

<h1>Selam yoldaş!</h1>

Devrim için ihtiyacın olan her şey burada

<ProductList/>

@code
{
    [Parameter]
    public string? CategoryUrl { get; set; }

    protected override async Task OnParametersSetAsync()
    {
        await ProductService.GetProducts(categoryUrl: CategoryUrl);
    }
}
